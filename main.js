const quarterValue = (quartered) => {
    quarter = quartered / 4
    return quarter
    }
    
    // Invoke the function and store the return value
    
    const returnedNumber = quarterValue(16)
    // Log the return value
    console.log(returnedNumber)